# Versioning Guide

- Increment patch for backward-compatible payload tweaks.
- Increment minor when new optional fields are added.
- Increment major for breaking schema changes.

Register overlapping handler ranges during migration windows.
